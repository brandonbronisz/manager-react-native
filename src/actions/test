$utm_source = [
      '500395',
      '500675',
      '500821'
  ];

  $utm_campaign = [
      'lg_beyond',
      'lg_j2c',
      'fr_d2_j2c',
      'fr_d3_j2c',
      'fr_d4_j2c'
  ];

  $provider = 'Hook';

  $job_provider = [
      'jobs2careers',
      'jobs2careersnwm',
      'beyond',
      'jobs2careersfdj'
  ];

  $search_text = [
      'doctor',
      'dispensary',
      'warehouse',
      'programmer',
      'pilot',
      'waiter',
      'clerk',
      'nurse',
      'mover',
      'janitor',
      'secretaries',
      'manager',
      'truck driver',
      'cvs',
      'teacher',
      'finance',
      'retail',
      'house keeper',
      'farmer',
      'carpenter',
      'customer service',
      'housekeeping',
      'lawyer'
  ];

  for ($i = 0; $i <= 100; $i++) {

      $int= mt_rand(1510876800,1510963199);

      $string = date("Y-m-d H:0:0",$int);

      \TheDMSGrp\Sms\Models\Reports\JobClickReport::create([

          'utm_source' => $utm_source[array_rand($utm_source)],
          'utm_campaign' => $utm_campaign[array_rand($utm_campaign)],
          'traffic_source' => '0000',
          'provider' => $provider,
          'job_provider' => $job_provider[array_rand($job_provider)],
          'search_text' => $search_text[array_rand($search_text)],
          'sent' => rand(0,10000),
          'delivered' => rand(0, 9999),
          'clicked' => rand(0, 500),
          'unsubscribes' => rand(0,500),
          'report_time' => $string

      ]);

  }
